Browser/Client                  Your Server              Sarvam AI           Azure Blob
     
     
     │                              │                        │                    │
     │── POST /api/translate-audio ─►                        │                    │
     │   (audio > 1MB)              │                        │                    │
     │                              │── initiateBatchJob() ──►                    │
     │                              │◄── { job_id } ─────────│                    │
     │                              │── getUploadUrl() ───────►                   │
     │                              │◄── { azure_sas_url } ───│                   │
     │                              │────────── uploadToAzure() ─────────────────►│
     │                              │◄─────────────────────── 200 OK ─────────────│
     │                              │── startBatchJob() ──────►                   │
     │◄── { jobId, status:"processing" }                                          │
     │                                                        │ (processing...)    │
     │── GET /api/check-job?jobId=X ►                        │                    │
     │                              │── getBatchJobStatus() ──►                   │
     │                              │◄── { job_state: "Running" }                 │
     │◄── { status: "processing" }  │                        │                    │
     │                              │   (10s later...)       │                    │
     │── GET /api/check-job?jobId=X ►                        │                    │
     │                              │── getBatchJobStatus() ──►                   │
     │                              │◄── { job_state: "Completed" }               │
     │                              │── getDownloadUrls() ────►                   │
     │                              │◄── { "https://azure.../0.json" }           │
     │                              │── fetch transcript ─────────────────────────►
     │                              │◄── { transcript: "Hello..." } ──────────────│
     │                              │ (translate + TTS...)   │                    │
     │◄── { originalText, translatedText, audioBase64 }                           │
